# Set this only when running Streamlit against the FastAPI service
# API_URL=http://localhost:8000

# Set this in API environments (docker/k8s) when using postgres
# DATABASE_URL=postgresql://user:password@postgres-service:5432/medical_db
